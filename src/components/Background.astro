<div class="animated-bg">
  <div class="subtle-texture"></div>
</div>

<style>
  .animated-bg {
    position: fixed;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
    z-index: -1;
    background: var(--color-bg);
  }

  .subtle-texture {
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 20% 30%, rgba(139, 115, 85, 0.03) 0%, transparent 50%),
      radial-gradient(circle at 80% 70%, rgba(107, 93, 82, 0.02) 0%, transparent 50%);
    opacity: 0.6;
  }

  :root[data-theme="monochrome"] .subtle-texture {
    background-image:
      radial-gradient(circle at 20% 30%, rgba(102, 102, 102, 0.02) 0%, transparent 50%),
      radial-gradient(circle at 80% 70%, rgba(85, 85, 85, 0.01) 0%, transparent 50%);
  }
</style>

---
const { title } = Astro.props;
const pageTitle = title || "setofr";
const currentYear = new Date().getFullYear();
---

<!doctype html>
<html lang="en" class="bg-[var(--color-bg)] text-[var(--color-text)]">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{pageTitle}</title>
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <link rel="stylesheet" href="/styles/global.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/simple-icons-font/font/simple-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=STIX+Two+Math&display=swap" rel="stylesheet">
  </head>
  <body class="antialiased">
    <div class="container mx-auto max-w-2xl px-4 py-8">
      <header class="flex justify-end items-center py-6">
        <nav class="flex space-x-4">
          <a href="/" class="nav-link">home</a>
          <a href="/blog" class="nav-link">blog</a>
          <a href="/projects" class="nav-link">projects</a>
        </nav>
      </header>
      <main class="mb-8">
        <slot />
      </main>
      <footer class="text-center py-4 border-t border-[var(--color-border)]">
        <p class="text-gray-500">© {currentYear} setofr</p>
      </footer>
    </div>
  </body>
</html>